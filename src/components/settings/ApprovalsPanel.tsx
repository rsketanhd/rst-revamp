import { SettingsPanel } from './SettingsPanel'

export function ApprovalsPanel() {
  return (
    <SettingsPanel
      title="Approvals"
      description="Configure approval workflows for offers, interviews, and customer changes."
    >
      {null}
    </SettingsPanel>
  )
}
